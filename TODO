Declare rpm_script_t, make it a typealias to the existing sys.isid as that
will hopefully satify RPM and allow me to remove the --setopt=tsflags=nocontexts
comment in README

	(tunable distro_redhat true)

	(tunableif distro_redhat

		(true
			(typealias rpm_script_t)
			(typealiasactual rpm_script_t sys.isid)))
